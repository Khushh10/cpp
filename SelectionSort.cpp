#include <iostream>
int main()
{
    int arr[6] = {40, 70, 30, 50, 10, 43};
    for (int i = 0; i < 6; i++)
    {
        if (p > 0 && arr[i] < arr[p])
        {
            
        }
    }

    return 0;
}