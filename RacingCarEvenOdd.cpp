#include <iostream>
using namespace std;

int main()
{
    int rno;
    for (int i = 1; i <= 5; i++)
    {
        if (i % 2 == 0)
        {
            int tr=0;
            for (int i = 0; i < tr; i++)
            {
                w
            }
            
            cout << "C1 wins"<<endl;
        }
        else
        {
            cout << "C2 wins"<<endl;
        }

    }

    return 0;
}